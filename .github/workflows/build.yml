name: Generate Play Store Bundle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Set up Java Development Kit (JDK 17)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Initialize Android Base Project
      run: |
        # settings.gradle بنانا
        cat <<EOF > settings.gradle
        include ':app'
        rootProject.name = "Ji-Bhai-Khata"
        EOF

        # مین پروجیکٹ کی build.gradle بنانا
        cat <<EOF > build.gradle
        buildscript {
            repositories { google(); mavenCentral() }
            dependencies { classpath 'com.android.tools.build:gradle:8.2.2' }
        }
        allprojects { repositories { google(); mavenCentral() } }
        EOF

        # ایپ کے فولڈرز تیار کرنا
        mkdir -p app/src/main/assets
        mkdir -p app/src/main/java/com/afuma/jibhaikhata
        
        # --- جادوئی کمانڈ: انڈیکس فائل کو کاپی کرنے سے پہلے اس کی CSS خودکار تبدیل کرنا ---
        # یہ کمانڈ 'overflow: hidden !important;' کو ختم کر دے گی تاکہ اسکرولنگ ہو سکے اور بٹن نظر آئیں
        sed -i 's/overflow: hidden !important;/overflow: visible !important;/g' index.html
        
        # آپ کی تبدیل شدہ انڈیکس فائل کو ایسٹس فولڈر میں منتقل کرنا
        cp index.html app/src/main/assets/index.html
        
        # اینڈرائیڈ مینی فیسٹ فائل بنانا (NoTitleBar تھیم کے ساتھ)
        cat <<EOF > app/src/main/AndroidManifest.xml
        <manifest xmlns:android="http://schemas.android.com/apk/res/android"
            package="com.afuma.jibhaikhata">
            
            <uses-permission android:name="android.permission.INTERNET" />
            <uses-permission android:name="android.permission.CALL_PHONE" />
            <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
            <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
            <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

            <application
                android:allowBackup="true"
                android:label="Ji Bhai Khata"
                android:supportsRtl="true"
                android:theme="@android:style/Theme.NoTitleBar">
                <activity android:name=".MainActivity" android:exported="true">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />
                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>
            </application>
        </manifest>
        EOF

        # MainActivity.java بنانا (موبائل سکرین سائز فکسڈ سیٹنگز کے ساتھ)
        cat <<EOF > app/src/main/java/com/afuma/jibhaikhata/MainActivity.java
        package com.afuma.jibhaikhata;

        import android.app.Activity;
        import android.content.Context;
        import android.content.Intent;
        import android.net.Uri;
        import android.os.Bundle;
        import android.print.PrintAttributes;
        import android.print.PrintDocumentAdapter;
        import android.print.PrintManager;
        import android.provider.AlarmClock;
        import android.webkit.WebChromeClient;
        import android.webkit.WebSettings;
        import android.webkit.WebView;
        import android.webkit.WebViewClient;
        import android.widget.Toast;

        public class MainActivity extends Activity {
            private WebView myWebView;

            @Override
            protected void onCreate(Bundle savedInstanceState) {
                super.onCreate(savedInstanceState);
                
                myWebView = new WebView(this);
                WebSettings webSettings = myWebView.getSettings();
                
                webSettings.setJavaScriptEnabled(true);
                webSettings.setDomStorageEnabled(true);
                webSettings.setAllowFileAccess(true);
                webSettings.setAllowContentAccess(true);
                webSettings.setDatabaseEnabled(true);
                
                // موبائل اسکرین سائز فٹ کرنے کی درست سیٹنگز (تاکہ زوم آؤٹ نہ ہو اور بٹن نیچے فکس رہیں)
                webSettings.setUseWideViewPort(false);
                webSettings.setLoadWithOverviewMode(false);
                
                myWebView.setWebChromeClient(new WebChromeClient());

                myWebView.setWebViewClient(new WebViewClient() {
                    @Override
                    public boolean shouldOverrideUrlLoading(WebView view, String url) {
                        if (url == null) return false;

                        if (url.startsWith("whatsapp:") || url.contains("api.whatsapp.com") || url.contains("wa.me")) {
                            return launchIntent(url, "WhatsApp is not installed");
                        }
                        if (url.startsWith("tel:")) {
                            return launchIntent(url, "Cannot open dialer");
                        }
                        if (url.startsWith("geo:") || url.contains("maps.google.com")) {
                            return launchIntent(url, "Maps app not found");
                        }
                        if (url.startsWith("alarm:")) {
                            try {
                                Intent intent = new Intent(AlarmClock.ACTION_SET_ALARM);
                                intent.putExtra(AlarmClock.EXTRA_MESSAGE, "جی بھائی کھاتا ریمائنڈر");
                                startActivity(intent);
                                return true;
                            } catch (Exception e) {
                                Toast.makeText(MainActivity.this, "Alarm App not found", Toast.LENGTH_SHORT).show();
                                return true;
                            }
                        }
                        return false;
                    }
                });

                // پرنٹنگ مینیجر
                myWebView.addJavascriptInterface(new Object() {
                    @android.webkit.JavascriptInterface
                    public void printPage() {
                        runOnUiThread(new Runnable() {
                            @Override
                            public void run() {
                                createWebPrintJob(myWebView);
                            }
                        });
                    }
                }, "AndroidPrint");

                myWebView.loadUrl("file:///android_asset/index.html");
                setContentView(myWebView);
            }

            private boolean launchIntent(String url, String errorMessage) {
                try {
                    Intent intent = new Intent(Intent.ACTION_VIEW, Uri.parse(url));
                    startActivity(intent);
                    return true;
                } catch (Exception e) {
                    Toast.makeText(MainActivity.this, errorMessage, Toast.LENGTH_SHORT).show();
                    return true;
                }
            }

            private void createWebPrintJob(WebView webView) {
                if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.KITKAT) {
                    PrintManager printManager = (PrintManager) this.getSystemService(Context.PRINT_SERVICE);
                    String jobName = "Ji_Bhai_Khata_Document";
                    
                    PrintDocumentAdapter printAdapter = webView.createPrintDocumentAdapter(jobName);
                    
                    if (printManager != null) {
                        printManager.print(jobName, printAdapter, new PrintAttributes.Builder().build());
                    }
                } else {
                    Toast.makeText(this, "Print not supported on this device version", Toast.LENGTH_SHORT).show();
                }
            }

            @Override
            public void onBackPressed() {
                if (myWebView.canGoBack()) {
                    myWebView.goBack();
                } else {
                    super.onBackPressed();
                }
            }
        }
        EOF

        # ایپ کی اپنی build.gradle فائل (ورژن 35 اور ورژن کوڈ 2 کے ساتھ)
        cat <<EOF > app/build.gradle
        plugins { id 'com.android.application' }
        android {
            namespace 'com.afuma.jibhaikhata'
            compileSdk 35
            defaultConfig {
                applicationId "com.afuma.jibhaikhata"
                minSdk 24
                targetSdk 35
                versionCode 2
                versionName "1.0.1"
            }
            signingConfigs {
                release {
                    storeFile file("release-key.jks")
                    storePassword "${{ secrets.KEYSTORE_PASSWORD }}"
                    keyAlias "${{ secrets.KEY_ALIAS }}"
                    keyPassword "${{ secrets.KEY_PASSWORD }}"
                }
            }
            buildTypes {
                release {
                    signingConfig signingConfigs.release
                    minifyEnabled false
                }
            }
        }
        EOF

    - name: Generate Secure Keystore
      run: |
        keytool -genkey -v -keystore app/release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias "${{ secrets.KEY_ALIAS }}" -storepass "${{ secrets.KEYSTORE_PASSWORD }}" -keypass "${{ secrets.KEY_PASSWORD }}" -dname "CN=JiBhaiKhata, O=Afuma, L=Lahore, S=Punjab, C=PK"

    - name: Create Gradle Wrapper
      run: gradle wrapper --gradle-version 8.5

    - name: Compile and Build APK & AAB Bundle
      run: |
        chmod +x gradlew
        ./gradlew app:bundleRelease app:assembleRelease

    - name: Output Upload - Google Play Store (.AAB)
      uses: actions/upload-artifact@v4
      with:
        name: Ji-Bhai-Khata-PlayStore-Bundle
        path: app/build/outputs/bundle/release/app-release.aab

    - name: Output Upload - Tester (.APK)
      uses: actions/upload-artifact@v4
      with:
        name: Ji-Bhai-Khata-Testing-APK
        path: app/build/outputs/apk/release/app-release.apk
